// Voiceflow ChatBot - DISABLED
// Replaced with custom DWSChatWidget component
// This file is kept for reference but ChatBot is no longer used

import React from "react";

const ChatBot = () => {
  // Voiceflow integration removed - using custom DWSChatWidget instead
  return null;
};

export default ChatBot;
