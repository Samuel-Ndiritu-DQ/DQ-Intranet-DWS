/**
 * Application Entry Point
 * 
 * Simple entry point that imports the main application.
 * MSAL v3.x is more stable and doesn't have the cache corruption issues of v4.x.
 */

// Import the main application  
import './main';
